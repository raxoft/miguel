# Database configuration.

development:
  adapter: sqlite

mysql:
  adapter: mysql2
  user: travis
  password: ''
  host: localhost
  database: miguel_test
  encoding: utf8

postgres:
  adapter: sqlite

# EOF #
